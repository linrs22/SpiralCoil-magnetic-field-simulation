# 阿基米德螺线线圈 — 使用说明

本目录包含：**自感/磁能估算**（`archimedean_spiral_inductance.py`）、**轴线与径向磁场 CSV 计算**（同一脚本 `--fields`）、以及**结果绘图**（`plot_coil_B_fields.py`）。

---

## 1. 环境准备

在终端进入本目录后执行：

```bash
pip install -r requirements.txt
```

### requirements.txt 全文

```
numpy>=1.20
matplotlib>=3.5
```

- **numpy**：磁场与自感数值计算。  
- **matplotlib**：仅 `plot_coil_B_fields.py` 画图需要；若只算场或自感，理论上可只装 numpy，但建议按 `requirements.txt` 一次装全。

---

## 2. `archimedean_spiral_inductance.py`

### 2.1 计算自感（默认模式）

不加重参数时，对**命令行给定**的匝数、网格等做 **Biot–Savart + 磁能法** 估算自感。

```bash
python archimedean_spiral_inductance.py
```

常用可选参数（见脚本内 `argparse`）：

| 参数 | 含义 | 默认（示例） |
|------|------|----------------|
| `--n-turns` | 匝数 | 10 |
| `--n-theta` | 沿螺线 θ 方向分段数 | 400 |
| `--n-grid` | 磁能积分立方体网格每边划分数 | 33 |
| `--current` | 电流 (A) | 1.0 |
| `--cutoff-um` | Biot–Savart 距离下限 (μm)，抑制奇点 | 自动 |
| `--exclude-near-um` | 磁能积分时排除距导体过近的体元 | 关闭 |

示例：

```bash
python archimedean_spiral_inductance.py --n-turns 50 --n-theta 8000 --current 0.001
```

### 2.2 磁场 CSV（几何由 `coil_geometry.json` 提供）

加 **`--fields`** 时，从同目录下的 **`coil_geometry.json`** 读取螺线几何（见下表），计算磁场并在**本脚本同目录**写出三个 CSV。可选参数 **`--coil-config 路径`** 指定其它 JSON 文件；省略则默认使用脚本旁的 `coil_geometry.json`。

```bash
python archimedean_spiral_inductance.py --fields
python archimedean_spiral_inductance.py --fields --coil-config D:\path\to\my_coil.json
```

**`coil_geometry.json` 字段说明：**

| 键 | 含义 | 单位 |
|----|------|------|
| `r_start_mm` | 起始半径 | mm |
| `r_end_mm` | 终止半径 | mm |
| `wire_width_um` | 线宽（截面在径向法向的宽度） | μm |
| `coil_spacing_um` | **相邻匝导体之间的间隙**（边缘到边缘） | μm |
| `wire_height_um` | 导体截面高度（可选，默认 1） | μm |
| `current_mA` | 电流（可选，默认 1） | mA |
| `axis_z_min_mm` | 轴线采样 $z$ 下限（可选，默认 -1） | mm |
| `axis_z_max_mm` | 轴线采样 $z$ 上限（可选，默认 1） | mm |
| `axis_z_num_points` | 轴线采样点数（可选，默认 401） | 整数 $\geq 2$ |
| `radial_r_min_mm` | 径向线采样 $\rho$ 下限，对应点 $(\rho,0,z)$（可选，默认 0） | mm |
| `radial_r_max_mm` | 径向线采样 $\rho$ 上限（可选，默认 2.9） | mm |
| `radial_r_num_points` | 径向线采样点数（可选，默认 801） | 整数 $\geq 2$ |

其中 **$\rho$（JSON 里写作 `radial_r`）** 为线圈平面内到原点的距离；当前程序在 **$y=0$** 上取点，即沿 **$+x$ 轴** 的坐标 $x=\rho$。若省略上述采样键，行为与旧版默认（$z\in[-1,1]$ mm、$\rho\in[0,2.9]$ mm）一致。

匝数由 \(N=(R_\mathrm{end}-R_\mathrm{start})/(\text{线宽}+\text{间隙})\) 推出（心距 = 线宽 + `coil_spacing_um`）。

**生成文件：**

| 文件名 | 内容 |
|--------|------|
| `B_axis_z.csv` | 中心轴 $(0,0,z)$；$z$ 范围与点数由 JSON 中 `axis_z_*` 决定 |
| `B_radial_line_z0.csv` | 射线 $(\rho,0,0)$，$z=0$；$\rho$ 由 `radial_r_*` 决定 |
| `B_radial_line_z_probe.csv` | 同上射线，$z = z_{\mathrm{probe}}$（导体半高 + 0.5 μm） |

终端会打印体元数量、耗时、中心点 $B_z$ 等摘要。

**注意：** `--fields` 与默认自感模式**二选一**；若需同时得到自感与磁场，请分两次运行。

---

## 3. `plot_coil_B_fields.py`

在已成功运行 **`python archimedean_spiral_inductance.py --fields`** 且同目录下存在上述三个 CSV 后执行：

```bash
python plot_coil_B_fields.py
```

**生成 JPG（与脚本同目录）：**

| 文件 | 对应数据 |
|------|-----------|
| `fig_B_axis_z.jpg` | `B_axis_z.csv` |
| `fig_B_radial_line_z0.jpg` | `B_radial_line_z0.csv` |
| `fig_B_radial_line_z_probe.jpg` | `B_radial_line_z_probe.csv` |

若缺少 CSV，脚本会报错并提示先运行 `--fields`。

---

## 4. 建议工作流程

1. `pip install -r requirements.txt`  
2. `python archimedean_spiral_inductance.py --fields` → 得到三个 CSV  
3. `python plot_coil_B_fields.py` → 得到三张 JPG  

若还需**自感**数值，再运行不带 `--fields` 的命令并自行设置 `--n-turns` 等参数。

---

## 5. 文件一览（与本说明相关）

| 文件 | 作用 |
|------|------|
| `requirements.txt` | Python 依赖版本下限 |
| `coil_geometry.json` | `--fields` 磁场计算的几何与电流参数 |
| `archimedean_spiral_inductance.py` | 自感 / `--fields` 磁场 CSV |
| `plot_coil_B_fields.py` | 读取 CSV 输出 JPG |
| `使用说明.md` | 本说明 |

CSV/JPG 为运行后生成，可不纳入版本管理。
