# 阿基米德螺线线圈 — 使用说明

本目录包含：**自感/磁能估算**（`archimedean_spiral_inductance.py`）、**轴线与径向磁场 CSV 计算**（同一脚本 `--fields`）、以及**结果绘图**（`plot_coil_B_fields.py`）。

---

## 1. 环境准备

在终端进入本目录后执行：

```bash
pip install -r requirements.txt
```

### requirements.txt 全文

```
numpy>=1.20
matplotlib>=3.5
```

- **numpy**：磁场与自感数值计算。  
- **matplotlib**：仅 `plot_coil_B_fields.py` 画图需要；若只算场或自感，理论上可只装 numpy，但建议按 `requirements.txt` 一次装全。

---

## 2. `archimedean_spiral_inductance.py`

### 2.1 计算自感（默认模式）

不加重参数时，对**命令行给定**的匝数、网格等做 **Biot–Savart + 磁能法** 估算自感。

```bash
python archimedean_spiral_inductance.py
```

常用可选参数（见脚本内 `argparse`）：

| 参数 | 含义 | 默认（示例） |
|------|------|----------------|
| `--n-turns` | 匝数 | 10 |
| `--n-theta` | 沿螺线 θ 方向分段数 | 400 |
| `--n-grid` | 磁能积分立方体网格每边划分数 | 33 |
| `--current` | 电流 (A) | 1.0 |
| `--cutoff-um` | Biot–Savart 距离下限 (μm)，抑制奇点 | 自动 |
| `--exclude-near-um` | 磁能积分时排除距导体过近的体元 | 关闭 |

示例：

```bash
python archimedean_spiral_inductance.py --n-turns 50 --n-theta 8000 --current 0.001
```

### 2.2 磁场 CSV（固定几何 + 1 mA）

加 **`--fields`** 时，按脚本内写死的**课题几何**（3–3.5 mm 阿基米德螺线、5×1 μm 截面、约 50 匝等）计算磁场，并在**本脚本同目录**写出三个 CSV（**不经过**上面的 `--n-turns` 等参数，几何在 `run_field_study_default_output` 中固定）。

```bash
python archimedean_spiral_inductance.py --fields
```

**生成文件：**

| 文件名 | 内容 |
|--------|------|
| `B_axis_z.csv` | 中心轴 $(0,0,z)$，$z \in [-1,1]$ mm，`Bx,By,Bz` (T) |
| `B_radial_line_z0.csv` | 射线 $(r,0,0)$，$r \in [0, 2.9]$ mm，$z=0$；平面电流下 $B_x=B_y=0$，主要读 $B_z$ |
| `B_radial_line_z_probe.csv` | 同上射线，$z = z_{\mathrm{probe}}$（导体半高 + 0.5 μm），含非零 $B_\rho=\sqrt{B_x^2+B_y^2}$ |

终端会打印体元数量、耗时、中心点 $B_z$ 等摘要。

**注意：** `--fields` 与默认自感模式**二选一**；若需同时得到自感与磁场，请分两次运行。

---

## 3. `plot_coil_B_fields.py`

在已成功运行 **`python archimedean_spiral_inductance.py --fields`** 且同目录下存在上述三个 CSV 后执行：

```bash
python plot_coil_B_fields.py
```

**生成 JPG（与脚本同目录）：**

| 文件 | 对应数据 |
|------|-----------|
| `fig_B_axis_z.jpg` | `B_axis_z.csv` |
| `fig_B_radial_line_z0.jpg` | `B_radial_line_z0.csv` |
| `fig_B_radial_line_z_probe.jpg` | `B_radial_line_z_probe.csv` |

若缺少 CSV，脚本会报错并提示先运行 `--fields`。

---

## 4. 建议工作流程

1. `pip install -r requirements.txt`  
2. `python archimedean_spiral_inductance.py --fields` → 得到三个 CSV  
3. `python plot_coil_B_fields.py` → 得到三张 JPG  

若还需**自感**数值，再运行不带 `--fields` 的命令并自行设置 `--n-turns` 等参数。

---

## 5. 文件一览（与本说明相关）

| 文件 | 作用 |
|------|------|
| `requirements.txt` | Python 依赖版本下限 |
| `archimedean_spiral_inductance.py` | 自感 / `--fields` 磁场 CSV |
| `plot_coil_B_fields.py` | 读取 CSV 输出 JPG |
| `使用说明.md` | 本说明 |

CSV/JPG 为运行后生成，可不纳入版本管理。
